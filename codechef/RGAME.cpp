#include <bits/stdc++.h>
using namespace std;
main()
{
    int T;
    cin>>T;
    while(T--)
    {
        int N;
        cin>>N;
        int a[N+1];
        for( int i=0;i<=N;i++)

        {
            cin>>a[i];
        }

        
    }
}